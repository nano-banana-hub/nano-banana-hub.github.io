name: Build Catalog

on:
  schedule:
    - cron: '0 6 * * *'
  push:
    paths:
      - 'catalog-source.json'
      - 'scripts/build-catalog.js'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Build catalog assets
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: node scripts/build-catalog.js

      - name: Check for generated changes
        id: check
        run: |
          if git diff --quiet -- catalog.json llms.txt agent-catalog.md robots.txt sitemap.xml; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Commit and push
        if: steps.check.outputs.changed == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add catalog.json llms.txt agent-catalog.md robots.txt sitemap.xml
          git commit -m "chore: rebuild BananaHub catalog assets [skip ci]"
          git push
